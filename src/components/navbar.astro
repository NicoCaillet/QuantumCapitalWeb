<nav class="navbar main-layout-overlay xl:max-w-[1300px] max-w-[1000px]">
  <!-- Logo -->
  <div class="navbar-logo">
    <a href="/">
      <img src="/Logo Quantum.png" alt="Logo de la Marca" />
    </a>
  </div>

  <ul role="list" class="navbar-links">
    <li class="navbar-item">
      <a class="dropdown-trigger text-white">About Us +</a>
      <!-- Subenlaces -->
      <div class="dropdown-menu">
        <a href="/quantum">Quantum Capital</a>
        <a href="/investment-manager">Investment Manager</a>
      </div>
    </li>
    <li><a href="/performance">Performance</a></li>
    <li><a href="/releases">Press Release</a></li>
  </ul>
</nav>

<style>
  .navbar {
    position: absolute; /* Posición absoluta */
    top: 0; /* Alineado al borde superior */
    left: 50%; /* Centro relativo al contenedor */
    transform: translateX(-50%); /* Corrige el desplazamiento del 50% */
    width: 100%; /* Asegura que abarque todo el ancho disponible */
    background: transparent; /* Fondo transparente */
    display: flex; /* Flexbox para alinear los elementos */
    align-items: center; /* Centra verticalmente */
    justify-content: space-between; /* Espaciado entre elementos */
    z-index: 3; /* Asegura que esté por encima del resto */
    padding: 20px 0;
  }

  .navbar-logo img {
    height: 50px;
    width: auto;
  }

  .navbar-links {
    display: flex;
    gap: 3rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .navbar-links a {
    text-decoration: none;
    color: white;
  }

  /* Estilo para el enlace principal */
  .navbar-item {
    position: relative; /* Necesario para posicionar los subenlaces */
  }

  .dropdown-trigger {
    cursor: pointer;
  }
  /* Subenlaces ocultos inicialmente */
  .dropdown-menu {
    position: absolute;
    top: 100%; /* Comienza justo debajo del enlace principal */
    left: 0;
    display: flex;
    flex-direction: row;
    width: 300px;
    gap: 1.5rem;
    background-color: transparent;
    padding-top: 10px;
    border-radius: 5px;
    opacity: 0; /* Comienza invisible */
    transform: translateY(-10px); /* Empieza ligeramente hacia arriba */
    transition:
      opacity 0.3s ease,
      transform 0.3s ease; /* Efecto de transición */
    pointer-events: none; /* Evita interacciones mientras está oculto */
  }

  .dropdown-menu a {
    color: white;
    text-decoration: none;
  }

  /* Mostrar subenlaces al hacer hover */
  .navbar-item:hover .dropdown-menu {
    display: flex;
    opacity: 1; /* Aparece gradualmente */
    transform: translateY(0); /* Baja suavemente */
    pointer-events: auto; /* Permite interacciones */
  }

  /* Efecto de hover en subenlaces */
  .dropdown-menu a:hover {
    text-decoration: none;
  }
</style>
