<!-- ---
const { data, setSelectedYear } = Astro.props;
---

<div class="grid grid-cols-4 gap-4 mt-10">
  {
    data.map((item) => (
      <div
        class="bg-white p-4 flex flex-col items-center h-[320px] border-2 border-black justify-center space-y-10 hover:bg-black hover:text-white group cursor-pointer"
        onClick={() => setSelectedYear(item.year)}
    
        {item.percentage && !item.thisYear ? (
          <div class="text-[80px] font-semibold text-green-700 group-hover:text-white">
            +{item.percentage}%
          </div>
        ) : (
          <p class="text-3xl mt-20">ooo</p>
        )}
        <div class="flex justify-center items-end space-x-2">
          <p class="-rotate-90 text-6xl w-1/4 font-semibold">{item.year}</p>
          <p class="text-4xl font-semibold text-left">
            Net Asset Values and Returns
          </p>
        </div>
      </div>
    ))
  }
</div> -->
